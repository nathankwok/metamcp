# MetaMCP Supabase + Cloud Run Deployment Configuration
# Copy this file to .env.deployment and fill in your values

# Required: Your Google Cloud Project ID
PROJECT_ID="your-gcp-project-id"

# Required: Supabase PostgreSQL connection string (get from Supabase Dashboard → Settings → Database)
# Make sure it uses port 6543 (pooler) not 5432 (direct)
SUPABASE_CONNECTION_STRING="postgresql://postgres.[ref]:[password]@aws-0-us-east-1.pooler.supabase.com:6543/postgres"

# Optional: Cloud Run region (default: us-central1)
REGION="us-central1"

# Optional: Environment name (default: production)
ENVIRONMENT="production"

# Instructions:
# 1. Copy this file: cp .env.deployment.example .env.deployment
# 2. Edit .env.deployment with your actual values
# 3. Run: ./supabase-cloudrun-deploy.sh
#
# Security Note: 
# - .env.deployment is already in .gitignore
# - Never commit database credentials to version control